# AGENTS.md

이 프로젝트에서 작업하는 모든 에이전트(pi 등)를 위한 지침입니다.

## 1. Markdown 작성: mermaid 사용 우선

Markdown 문서를 만들 때, **다이어그램으로 이해를 돕는 가치가 확실하다면 mermaid를 사용**하세요.

- 흐름, 구조, 관계, 상태 전이, 아키텍처, 순서 등 **시각화가 명확히 이해를 돕는 경우** mermaid 코드 블록(` ```mermaid `) 사용
- 단순 나열이나 짧은 설명은 일반 텍스트 유지 (불필요한 다이어그램 금지)
- mermaid가 오히려 혼란을 주는 경우(단순한 목록)엔 쓰지 말 것
- 예: 아키텍처는 `flowchart`/`graph`, 데이터 흐름은 `sequenceDiagram`, 일정은 `gantt`

**원칙**: 명확한 가치가 있을 때만. 장식용으로 쓰지 않는다.

## 2. 프로젝트 개요

- 한국 money market을 주무대로 한 전 세계 주요국 금융·경제 지표 모니터링 시스템
- 무료 데이터 소스만 사용, 웹 대시보드 중심, 에이전트(pi) 연동 목표
- 상세는 `docs/README.md` 참조

## 3. 문서 구조 (이 프로젝트의 규칙)

모든 계획·문서는 `docs/` 아래에 구조화한다:

```
docs/
├── README.md            # 전체 개요 + 문서 맵
├── architecture/        # 시스템 구조, 데이터 흐름
├── sources/             # 데이터 소스, 가입 절차, 인증키
├── plan/                # 마일스톤 개발 계획
├── tasks/               # 실행 작업 체크리스트 (M0, M1, ...)
└── roadmap/             # 최종 목표까지 로드맵
```

- 새 계획은 `docs/plan/` 또는 `docs/tasks/`에 추가
- 마일스톤은 `docs/plan/README.md`의 M0~M7 체계를 따름

## 4. 핵심 원칙

- **무료로만**: 유료 데이터 소스로 전환하지 않는다
- **MVP 우선**: 완성형을 기다리지 말고, 매 단계 동작하는 것부터 만든다
- **한 곳에 모든 데이터**: 단일 저장소(SQLite), 웹과 에이전트가 공유
- **에이전트 연동**: 처음부터 REST API / MCP 설계 염두
- **Ponytail**: 표준 먼저, 새 의존성 최소화, 불필요한 추상화 금지

## 5. 작업 규칙

- 진행 중인 마일스톤은 `docs/tasks/`의 체크리스트로 추적
- 작업 후 체크리스트 상태를 갱신
- API 키는 소스코드에 넣지 말고 환경변수 또는 keychain에 보관